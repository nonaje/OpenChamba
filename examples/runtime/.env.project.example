# Runtime por proyecto (opcional)
#
# Uso sugerido:
#   cp examples/runtime/.env.project.example .env.mi-proyecto
#   docker compose --env-file .env.mi-proyecto up -d
#
# Mantiene un token/puertos independientes por proyecto cuando corres varios stacks.

COMPOSE_PROJECT_NAME=open-chamba-mi-proyecto

# API OpenCode / UI OpenChamber
OPENCODE_SERVER_PORT=4096
OPENCHAMBER_PORT=3000

# Exec-bridge
RESTART_BRIDGE_PORT=8080
OPENCODE_CONTROL_TOKEN=replace-with-a-long-random-secret
OPENCHAMBER_EXTERNAL_RESTART_TOKEN=
OPENCHAMBER_EXTERNAL_RESTART_TIMEOUT_MS=15000
DOCKER_REQUEST_TIMEOUT_MS=5000
